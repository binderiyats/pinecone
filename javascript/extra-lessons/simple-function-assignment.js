const ner = (firstName) => {
    console.log(firstName);
}
ner("Binderiya");

const fullName = (lastName, firstName) => {
    return firstName +" "+ lastName;
}
const full = fullName('Tsogt','Binderiya')
console.log(full);


const hamaagu = "i want macbook air";
const toUpper = (text)=>{
    const upperCased = text.toUpperCase();
    console.log(upperCased);
}
toUpper(hamaagu);

const sum = 0;
const randomNumbers = (a,b) => {
    return a + b;
}
console.log(randomNumbers(1,2));

