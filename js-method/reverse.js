//reverse number
        let inputToo = window.prompt("too bichne uu");
      function reverse(data) {
        return Number(data.split("").reverse().join(""));
      }
        console.log(reverse(inputToo));